// Single-column, text-only resume, built to survive an ATS text extractor: no
// tables, no images, no sidebars, standard section headings, and a font family
// (Liberation Sans, metrically Arial) that PDF extractors round-trip.
//
// This is the default and the reference. If another template scores worse on
// the ATS gate, this is the one it is worse than.

#import "/common.typ": resume

#resume()
